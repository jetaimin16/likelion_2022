// 원시 타입(기본 자료형, 기본형)
let number = 1; // 숫자형(number)
let str = "abe"; // 문자열형(string)
let bool = true; // 논리형(boolean)
let undi = undefined; // undefined
let nul = null; // null(object)
let symbol = Symbol(); // symbol - 거의 사용 X

console.log(typeof(null));

// 참조 타입(참조 자료형, 참조형)
let array = []; // 배열(object)
let obj = {}; // 객체(object)
let func = function(){}; // 함수(function)

console.log(typeof(function(){}))